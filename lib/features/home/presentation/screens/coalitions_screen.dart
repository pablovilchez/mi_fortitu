import 'package:flutter/material.dart';

class CoalitionsScreen extends StatelessWidget {
  const CoalitionsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Coalitions'),
      ),
      body: Center(
        child: Text('Coalitions Screen'),
      ),
    );
  }
}